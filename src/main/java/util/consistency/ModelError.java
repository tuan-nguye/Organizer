package util.consistency;

public enum ModelError {
    // possible folder errors
    INVALID_FOLDER_NAME,
    INVALID_FOLDER_STRUCTURE,
    FOLDER_ABOVE_THRESHOLD,
    FILES_IN_NON_LEAF,
    FOLDER_CONTAINS_INCONSISTENT_DATES
}
